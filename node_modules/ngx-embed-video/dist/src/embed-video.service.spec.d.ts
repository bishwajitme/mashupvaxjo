import { ApiHttp, ApiService } from '../src';
export interface User {
    id?: string;
    name?: string;
}
export declare class UsersService extends ApiService<User> {
    protected http: ApiHttp;
    constructor(http: ApiHttp);
}
